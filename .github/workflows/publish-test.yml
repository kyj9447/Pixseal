name: Publish to Test PyPI

on:
  workflow_dispatch:

jobs:
  build-wheels-linux:
    name: Build wheels on Linux
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build wheels (Linux)
        uses: pypa/cibuildwheel@v2.21.3
        with:
          package-dir: pip_package
          output-dir: wheelhouse

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: test-wheels-linux
          path: pip_package/wheelhouse/*.whl

  build-wheels-windows:
    name: Build wheels on Windows
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build wheels (Windows)
        uses: pypa/cibuildwheel@v2.21.3
        with:
          package-dir: pip_package
          output-dir: wheelhouse

      - name: Upload wheel artifacts
        uses: actions/upload-artifact@v4
        with:
          name: test-wheels-windows
          path: pip_package/wheelhouse/*.whl

  build-sdist:
    name: Build sdist
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Build source distribution
        working-directory: pip_package
        run: |
          python -m pip install build
          python -m build --sdist --outdir ../dist

      - name: Upload sdist artifact
        uses: actions/upload-artifact@v4
        with:
          name: test-sdist
          path: dist/*.tar.gz

  publish:
    name: Upload to Test PyPI
    runs-on: ubuntu-latest
    needs:
      - build-wheels-linux
      - build-wheels-windows
      - build-sdist
    environment:
      name: testpypi
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: test-*
          path: dist
          merge-multiple: true

      - name: Publish to Test PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          # 1. URL 형식을 올바르게 수정 (https:// 포함)
          repository-url: test.pypi.org
          # 2. API 토큰 방식 사용 (사용자명은 자동으로 __token__이 사용됨)
          password: ${{ secrets.TEST_PYPI_API_TOKEN }}
